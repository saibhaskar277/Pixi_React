import type { CapacitorConfig } from "@capacitor/cli";

const config: CapacitorConfig = {
  appId: "com.yourcompany.pixireactgame",

  appName: "Pixi React Game",

  webDir: "dist",

  android: {
    backgroundColor: "#000000",
  },
};

export default config;
